{
  "name": "@onekeyhq/server",
  "version": "2.0.14",
  "description": "Server",
  "private": true,
  "scripts": {
    "lint": "tsc --noEmit && eslint . --ext .ts,.tsx",
    "lint:fix": "tsc --noEmit && eslint . --ext .ts,.tsx --fix"
  }
}
